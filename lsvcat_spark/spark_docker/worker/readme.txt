Worker image creation by setting up Spark Application to run as a worker node
Similar to the master node, we configure the ports to expose the worker web ui *



*Note SPARK_MASTER_HOST is the env variable configured in the base/spark .dockerfile