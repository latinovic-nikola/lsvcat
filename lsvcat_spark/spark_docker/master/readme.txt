Master image creation by setting up Spark Application to run as a master node
Also, network port is configured to allow the connection with worker nodes and to expose the the master web UI *



*Note SPARK_MASTER_PORT is the env variable configured in the base/spark .dockerfile